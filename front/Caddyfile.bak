DOMAIN {
    root * /src/dist

    file_server

    TLS

    handle /static/config.js {
        header Cache-Control "no-cache, no-store, must-revalidate, max-age=0"
        header Pragma "no-cache"
        header Expires "0"
        header Surrogate-Control "no-store"
        file_server
    }
}
